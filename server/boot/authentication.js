'use strict';

module.exports = function enableAuthentication(server) {
  // enable authentication
  server.enableAuth();

 
      //});
      //m.replaceOrCreate(mongoData, [{'validate':true}], null)
    
      
    //};

   
   //console.log (mongoXlsx.xlsxData2MongoData(mongoData,"mapper"));

};
